@page
@model ControlDeGastos.Pages_FondoMonetario.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FondoMonetarioModel[0].nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FondoMonetarioModel[0].tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FondoMonetarioModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tipo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.rowid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.rowid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.rowid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
