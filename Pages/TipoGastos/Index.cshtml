@page
@model ControlDeGastos.Pages_TipoGastos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Codigo @* @Html.DisplayNameFor(model => model.TipoGastoModel[0].rowid) *@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoGastoModel[0].nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoGastoModel[0].Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TipoGastoModel) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.rowid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nombre)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.rowid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.rowid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.rowid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
